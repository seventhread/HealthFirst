# HealthFirst 单角色提醒交互分镜（MVP）

版本：0.5  
原则：角色静态方向已经确认；现在用统一的道具语法把出现、等待、陪伴和退场做成可实现的正式动作。

## 1. 设计顺序

灰盒阶段已经完成，正式主角方向见 [CHARACTER_DESIGN.md](CHARACTER_DESIGN.md)。登场、同意和站立 60 秒黄金流程已进入正式运行实现；当前继续验证：

1. 角色从哪里出现，是否像在对用户说话。
2. 三个选择是否一眼可懂，点击后的反馈是否自然。
3. 开始后，角色陪伴相应秒数是否舒服而不打扰。
4. 用户暂时不做或没有回应时，退场和再次返回是否既有趣又不烦。
5. 默认两次出现与主动开启的认真模式，边界是否清楚。

当前生产顺序为：

1. 已冻结正式角色的静态轮廓、比例、两种表情和橙色织带道具语言。
2. 已完成首次进入、同意与站立 60 秒的正式运行纵切，并验证安全操作层不随动作移动。
3. 下一批把稍后、跳过、无回应和二次返回替换为正式关键姿势。
4. 随后补齐护眼、安静练习与认真模式中的角色协作，再统一打磨卡片视觉皮肤。

具体微互动见 `MICROINTERACTION_CONCEPTS.md`。正式实现采用透明关键姿势与原生 SwiftUI 道具混合渲染；业务状态不等待动画完成，也不制作难以降级的长逐帧序列。

## 2. 一轮提醒总览

```text
到期
  → 首次出现：角色入场 + 说一句话
      ├─ 开始 → 角色确认 → 陪伴倒计时 → 完成动作 → 退场
      ├─ 稍后 → 角色确认返回时间 → 中性退场 → 新 deadline
      ├─ 本次跳过 → 角色平静收尾 → 本轮结束
      └─ 无回应 → 角色做淡出动作 → pending → 约 3 分钟后再次返回
          → 再次出现：新台词 + 新动作/道具
              ├─ 开始 → 陪伴倒计时 → 完成
              ├─ 稍后 → 中性退场 → 新 deadline
              ├─ 本次跳过 → 本轮结束
              └─ 无回应 → 退回菜单栏，本轮不再主动弹出
```

认真模式由用户主动开启，只在第二次仍无回应后增加一次柔和全屏呈现；之后仍不无限追逐。

## 3. 首次出现

目标：角色像刚来到用户身边，用一句话发出低成本邀请。

```text
0 ms      小窗在当前屏幕右上角淡入，不抢键盘焦点
80 ms     主角沿橙色织带从菜单栏方向进入
250 ms    角色完成一次与提醒类型有关的短动作
350 ms    对话气泡与全部按钮可操作
0–8 s     角色安静等待，不循环弹跳
8 s       无悬停、无键盘聚焦、无操作时开始退场
8.4 s     小窗关闭，菜单栏显示一枚安静的待处理书签
```

按钮固定为：`开始 20/30/60 秒`、`3 分钟后提醒`、`本次跳过`。

角色正文最多两行，只承担“做什么 + 多久”这一项职责。按钮不使用角色口吻，避免用户每次重新理解操作。

## 4. 用户选择后的分支

### 4.1 选择“开始”

```text
0 ms      按钮立即禁用并幂等写入 guided 状态
0–250 ms  角色做一次 acknowledgeStart 动作；正文收拢为动作提示
≤400 ms   进入陪伴倒计时，不等待动画回调
进行中    角色留在屏幕边缘，以低动态动作陪伴
结束      写入 completed，播放 0.5–1 秒完成动作后关闭
```

三类提醒的陪伴方式：

| 提醒 | 陪伴时长 | 角色职责 | 屏幕表现 |
|---|---:|---|---|
| 护眼 | 20 秒 | 把提醒卷成“窗帘”并主动让出视线，结束时回来 | 只留边缘结束提示；不能要求用户盯着倒计时 |
| 站立 | 60 秒 | 分阶段拆下已读信息与装饰框，收成一辆 UI 小推车 | 倒计时与提前结束固定；允许用户离开工位 |
| 小动作 | 30 秒 | 把正文折成无字隐私信封，然后完整转过身 | 不出现具体身体部位，也不模仿、计次或判断动作 |

完整倒计时是 MVP 的完成记录依据；产品不宣称已经通过传感器验证真实动作。

三类陪伴的正式关键帧见 [站立动作](CHARACTER_DESIGN.md#7-站立-60-秒ui-小推车)、[护眼动作](CHARACTER_DESIGN.md#8-护眼-20-秒把屏幕内容卷走) 和 [安静练习动作](CHARACTER_DESIGN.md#9-安静练习-30-秒中性隐私信封)。

### 4.2 选择“稍后”

```text
0 ms      以点击时刻计算新的 deadline
0–600 ms  角色把提醒折成预约票并夹回菜单栏，显示“好，3 分钟后再来”
≤600 ms   小窗关闭
```

稍后是用户主动安排，不升级语气、不累积责备。后续可允许 2/5/10 分钟选项，MVP 固定为 3 分钟。

### 4.3 选择“本次跳过”

```text
0 ms      幂等写入 skipped，本轮结束
0–500 ms  角色平静收尾并退场
```

不问理由，不表现伤心，不把跳过写成失败。下一周期从跳过时刻重新计算。

### 4.4 没有回应

```text
8 s       正文淡出，角色只收回自己的装饰道具
8–8.4 s   提醒框折成待处理书签，角色沿原路淡出
淡出后    状态进入 retryPending；菜单栏保留待处理书签
约 3 min  使用新文案和新动作再次返回
```

无回应不等于拒绝，更不等于完成。退场可以有一点戏，但不能出现哭泣、受伤、发怒或“我看到你没做”的监视感。

## 5. 再次返回

第二次不是把同一弹窗复制一遍，也不是更红、更响；变化来自新的台词、动作或道具。

正式关键帧见 [第二次返回动作分镜](assets/character/mascot-followup-storyboard-v1.png)。

```text
0 ms      在同一位置和尺寸淡入
80 ms     角色以 enterFollowup 动作返回
250 ms    新道具或新动作说明“这件事还在”
350 ms    新正文与固定位置按钮可操作
0–10 s    等待选择，不循环制造注意力
10 s      仍无回应则中性退场；只在菜单栏保留 pending
```

按钮保持：`现在开始`、`10 分钟后提醒`、`本次跳过`。

第二次文案只承接事实并降低门槛，例如“刚才没赶上，现在用 20 秒处理掉？”；不能使用“终于”“又不理我”等语言。

## 6. 柔和遮罩与认真模式

默认小窗可以带局部柔和遮罩，但不改变显示器亮度、不闪烁、不让文字无法辨认。

正式关键帧见 [认真模式动作分镜](assets/character/mascot-serious-mode-storyboard-v1.png)。

认真模式必须由用户在设置里主动开启：

```text
0 ms      紧急跳过先进入最上层并可操作
0–350 ms  角色铺开全屏柔和遮罩
350 ms    居中对话气泡与角色出现
≤500 ms   “现在开始”可操作；始终展示“紧急跳过”
持续      不锁键盘鼠标，不移动按钮，不抢当前输入焦点
退出      紧急跳过后 200 ms 内先撤掉遮罩，再清理角色动画
```

第三次仍无回应就结束本轮，不无限升级。

用户选择开始后，认真模式界面退出，再交由对应的 20/30/60 秒陪伴动作接管；大遮罩不在整个引导期间持续存在。

## 7. 语义动作表

正式角色无论长什么样，都只接收这些语义动作：

| 场景 | `CharacterAction` | 角色表达 | 最大时限 |
|---|---|---|---:|
| 首次进入 | `enterFirst` | 来到用户身边并邀请 | 350 ms 后可操作 |
| 再次返回 | `enterFollowup` | 带着新上下文回来 | 350 ms 后可操作 |
| 等待 | `awaitResponse` | 安静看向选择区 | 不循环抢注意力 |
| 开始 | `acknowledgeStart` | 确认并让出视觉中心 | 400 ms |
| 稍后 | `acknowledgeSnooze` | 中性确认返回时间 | 600 ms |
| 跳过 | `acknowledgeSkip` | 平静收尾 | 500 ms |
| 无回应 | `exitNoResponse` | 做一次短动作后淡出 | 400 ms |
| 陪伴 | `guide(kind)` | 按提醒类型提供低动态陪伴 | 20/30/60 秒 |
| 完成 | `complete` | 单次正反馈 | 1 秒 |
| 紧急退出 | `emergencyExit` | 立即让路 | 200 ms 撤遮罩 |

业务状态不得等待动画结束。缺少精确动作时，依次使用通用动作、静态角色、无角色纯文字界面。

## 8. 文案、动作与皮肤的配对

三条选择链彼此独立：

```text
文案：kind + stage + tone → PromptCopy
动作：interactionState + userChoice + kind → CharacterAction
皮肤：CharacterAction + skinID → AssetDescriptor
```

- 文案不能引用动画 ID，也不决定重试时间。
- 皮肤不能包含文案、按钮标签、频率或状态迁移。
- 同一句或同一梗当天不重复；同一文案至少间隔 5 次使用。
- 第二次提醒不得复用本次首次出现的正文和动作变体。
- 盆底肌提醒必须使用隐私文案和不暴露身体部位的道具。
- VoiceOver 只朗读提醒、倒计时状态和操作，不朗读装饰动画细节。

## 9. Reduce Motion 替代

| 默认动作 | Reduce Motion 替代 |
|---|---|
| 从菜单栏方向进入 | 原位淡入 |
| 弹跳、伸缩、旋转 | 静态表情或道具切换 |
| 遮罩缩放 | 颜色与透明度渐变 |
| 完成粒子 | 单次高亮或勾选状态 |
| 横向退场 | 原位淡出 |

所有替代仍表达状态变化，不能因为关闭动画而失去反馈。

## 10. 流程验收

- 正式主角可以完整演示首次、开始、稍后、跳过、无回应、再次返回、陪伴、完成和紧急退出。
- 每个按钮点击后立即进入唯一业务状态；双击不会产生重复记录。
- 护眼陪伴不要求用户看着屏幕，站立允许用户离开工位。
- 第二次返回的文案和动作都与第一次不同，按钮位置不变。
- 角色动画缺失、卡住或被关闭时，纯文字流程仍完整可用。
- 鼠标悬停或键盘聚焦时，自动淡出暂停。
- 提醒不抢输入焦点；Esc 始终可以退出遮罩。
- 深浅色、键盘、VoiceOver、Increase Contrast、Reduce Transparency 与 Reduce Motion 分别验收。
- 双显示器、全屏 Space、锁屏与唤醒不会留下窗口或重复提醒。
