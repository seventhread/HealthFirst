# HealthFirst 单角色微互动设计

版本：0.7  
阶段：正式角色静态方向、同意动作与站立 60 秒黄金流程已经接入运行版

角色的形象锚点、表情边界和动作分镜见 [CHARACTER_DESIGN.md](CHARACTER_DESIGN.md)。本文继续维护与具体画风无关的状态语义、时序和实现边界。

## 1. 核心创意：提醒框不是容器，而是角色手里的道具

角色不只是站在提醒框旁边做表情。标题栏、正文纸片、按钮托盘、边框和进度环都像轻巧的实体零件，可以被角色拉开、折叠、盖章、收走和重新组装。

这样，趣味来自角色“处理提醒”这件事本身，而不是依赖夸张表情或不断更换段子。

设计约束：

- 角色操作的是 UI，不操控用户，也不评价用户。
- 动画只改变视觉，不改变提醒状态与按钮含义。
- 关键按钮不能被拿走：陪伴期间始终保留 `提前结束`，认真模式始终保留 `紧急跳过`。
- 角色把 UI 拆得越干净，用户需要关注屏幕的程度越低。
- 实际 `NSPanel` 尺寸不变化；“拆框”只发生在不可点击的装饰层，避免窗口跳动。
- 每段动作有清楚的结束点，不做持续弹跳、凝视或循环索取注意力。
- Reduce Motion 下改成零件原位淡出、静态折叠状态和文字回执。

## 2. 三种离开动画

### 2.1 稍后 3 分钟：把提醒折成预约票

情绪：收到安排，守时回来。

```text
0 ms      用户点击“稍后 3 分钟”，状态立即写为 snoozed
0–100 ms  角色从提醒框边缘抽出一条带虚线的时间标签
100–300 ms 提醒框沿虚线折成一张窄窄的预约票，显示“3:00”
300–520 ms 角色把预约票夹到菜单栏图标下方
≤600 ms   角色向菜单栏方向退场；菜单栏短暂显示“3 分钟后”
```

小巧思：角色离开前轻敲一下预约票，数字从 `3:00` 开始走，但不持续显示在桌面上。

边界：不使用“这次别再忘了”等语气；稍后是用户主动安排，不属于待处理警告。

Reduce Motion：提醒框原位淡出，菜单栏出现静态 `3 分钟后` 标签。

### 2.2 本次跳过：盖章、卷起、归档

情绪：这轮明确结束，平静收尾。

```text
0 ms      用户点击“本次跳过”，状态立即写为 skipped
0–100 ms  角色把提醒框翻成一张小工单
100–250 ms 在角落盖一次“本轮结束”章
250–450 ms 工单卷成细卷或折成小方片，滑入一个无红叉的归档槽
≤500 ms   角色做一次简短挥手后离开；不保留待处理书签
```

小巧思：归档槽只在动作发生时出现，吞下纸片后变成菜单栏里一条普通分隔线。

边界：不庆祝跳过，也不表现失望；不使用失败、扣分或连续记录归零。

Reduce Motion：显示 `本轮已跳过` 500 ms，随后整体淡出。

### 2.3 无回应：把提醒折成待处理书签

情绪：可能正忙，我先让路，但这件事还在。

```text
8 s       没有悬停、焦点或操作
0–100 ms  对话正文先淡出，角色开始收回自己的装饰道具
100–260 ms 空下来的提醒框四角向内折；操作栏最后才淡出
260–380 ms 折好的小方片收成一枚浅灰书签，被夹到菜单栏图标下方
≤400 ms   角色沿原路退场；状态为 retryPending
约 3 min  角色把书签从菜单栏取下，展开成新的提醒框
```

小巧思：第二次展开时，纸片的另一面是新文案，并带一个新道具；不是原弹窗原样回放。

边界：角色不能敲屏幕、叹气、垂头、回望用户、受伤或说“你没理我”。书签不闪烁、不显示未完成数字。鼠标、键盘或辅助功能焦点仍在提醒内时暂停自动淡出。

Reduce Motion：提醒框淡出，菜单栏书签淡入；再次返回时反向溶解。

## 3. 点击开始：把操作区变成陪伴工具

```text
0 ms      用户点击开始，状态立即写为 guided
0–160 ms  主按钮收拢成进度核心，其他选择淡出
160–400 ms 角色把按钮托盘推到提醒框下缘，变成倒计时轨道
≤400 ms   陪伴计时开始；`提前结束` 固定在轨道末端
```

开始动画只负责确认“我和你一起开始了”。业务不能等待角色推完托盘。

## 4. 站立 60 秒：角色逐步拆掉提醒框

目标：一开始给出清楚指令，随后让 UI 越来越轻。拆下来的零件不是消失，而是被角色收成一辆小推车，形成连续的小剧情。

正式角色关键帧见 [站立 60 秒陪伴动作分镜](assets/character/mascot-stand-60-storyboard-v1.png)。图中的深灰功能坞用于强调固定交互层，与浅灰可拆装饰层不是同一套零件。

| 时间 | UI 变化 | 角色动作 | 用户仍可用 |
|---:|---|---|---|
| 0–8 秒 | 完整框保留；显示动作提示与 60 秒进度 | 角色检查边框，像准备开始一项小工程 | 倒计时、提前结束 |
| 8–22 秒 | 标题条脱开，折成推车把手 | 角色把标题条轻轻拔下，装到身后 | 倒计时、提前结束 |
| 22–38 秒 | 正文纸片对折成小纸箱 | 角色把纸箱放上推车；屏幕只剩一句短提示 | 倒计时、提前结束 |
| 38–52 秒 | 左右装饰边框滑下，叠成推车底座 | 角色左右各收一条，动作不遮挡进度 | 倒计时、提前结束 |
| 52–60 秒 | 只剩进度核心和一条细轨道 | 角色扶着装满零件的小推车等待结束 | 倒计时、提前结束 |
| 完成 | 进度核心变为一次勾选，轨道合上 | 角色把勾选夹在最上面，推着整齐的 UI 零件离开 | 可立即关闭 |

小巧思：60 秒结束时，不放烟花；小推车因为多了一块“完成”牌而轻轻压低一下，角色扶稳后再走。

边界：被拆掉的都是装饰与已读信息，不能拆走进度、提前结束或键盘焦点。拆解不应制造“应用坏了”的错觉，每个零件都保持同一材质和圆角语言。

Reduce Motion：在 8/22/38/52 秒四个节点，信息层级原位淡化；右下角静态显示一叠已收好的零件。

## 5. 护眼 20 秒：把提醒框卷成窗帘

目标：提醒用户看远处，因此角色的最佳陪伴方式是主动把屏幕让出来。

正式角色关键帧见 [护眼 20 秒陪伴动作分镜](assets/character/mascot-eye-20-storyboard-v1.png)。

```text
0–1.1 秒  角色把对话框卷成软卷，并与软卷一起回到菜单栏
1.1–20 秒 屏幕边缘只留静态小标签与安全功能坞；不显示数字或中场动画
20 秒      角色与软卷返回，只展开一条无文字的窄完成牌
20–20.6 秒 角色轻微微笑，随后把完成牌一起收走
```

小巧思：角色卷窗帘前先把自己的脸也挡住，表达“我也不需要你看着我”。

始终可用：屏幕角落的 `提前结束`、键盘 Esc、结束提示音开关。

边界：20 秒内没有持续旋转的进度环，也不在结束前提前探头；鼠标悬停、键盘焦点或辅助功能查询时才显示剩余时间。

Reduce Motion：角色与提醒在 200 ms 内原位淡出，只保留静态功能坞；完成时窄完成牌原位淡入。

## 6. 小动作 30 秒：把提醒框封成隐私信封

目标：保留陪伴感，但不在共享屏幕或开放办公室暴露练习内容。

正式角色关键帧见 [安静练习 30 秒陪伴动作分镜](assets/character/mascot-quiet-30-storyboard-v1.png)。

```text
0–0.9 秒  卡片翻到无字背面并折成中性信封，橙色织带完成一次封口
0.9–30 秒 角色完整转过身，信封与安全功能坞静止显示
30 秒      角色转回三分之四侧面，信封折成无文字的小完成牌
30–31 秒  角色轻微微笑，随后把完成牌一起收走
```

小巧思：角色对隐私过于认真，干脆把整个身体转过去；不是害羞，也不是营造“秘密任务”的暧昧感。

边界：动作不模拟身体收缩，不使用暧昧表情，不暗示角色看见或判断了用户动作。30 秒只表示总时长，不提供收缩、放松或呼吸节奏。

屏幕共享、菜单栏和系统级提示只显示`安静练习`或`小动作`；信封上不出现文字、身体图示、锁或爱心。

Reduce Motion：内容卡直接交叉淡化为密封信封，角色在正面与背面静态姿势之间切换；完成牌原位淡入。

## 7. 再次返回：从书签展开另一面

正式角色关键帧见 [第二次返回动作分镜](assets/character/mascot-followup-storyboard-v1.png)。

```text
0–180 ms  橙色织带带着折叠角色回来，摘下待处理书签
180–360 ms 书签向下展开成纸条，空白背面朝外
360–500 ms 纸条成为完整卡片；角色停半拍
500–650 ms 角色把卡片翻到另一面，露出新文案和固定按钮
≤700 ms   三个固定位置的操作可用
```

这使“再次回来”成为第一次退场动作的后半段，而不是孤立的新动画。第二次卡片不变红、不放大；如果动画收尾期间用户已经操作，立即取消翻面动画并响应选择。

Reduce Motion：书签与正面卡片原位交叉淡化，不播放下拉、展开或三维翻面。

## 8. 完成反馈：把最后一块收好

完成反馈沿用当前陪伴隐喻，不额外切换成通用烟花：

- 站立：把完成牌放到 UI 小推车顶部，扶稳后推走。
- 护眼：把卷起的窗帘系好，露出一次小勾。
- 小动作：信封封口合上，出现一个低调印记。

反馈控制在 0.5–1 秒，可点击立即收起。偶尔可以更换一个动作变体，但不把每次完成都做成大型庆典。

## 9. 认真模式：铺开柔和工作垫

正式关键帧见 [认真模式动作分镜](assets/character/mascot-serious-mode-storyboard-v1.png)。

```text
0 ms      紧急跳过先进入最上层并可操作
0–200 ms  角色开始铺开柔和遮罩
200–350 ms 遮罩稳定，桌面仍可辨认
350–500 ms 中央正常尺寸提醒卡与主按钮出现
持续      角色静止；按钮与退出控制不移动
紧急退出  立即写入 emergencySkip，200 ms 内先撤遮罩，再清理角色
开始      立即写入 guided，撤认真模式界面后交给对应陪伴流程
```

小巧思：角色像铺一张远大于自己的工作垫一样，把遮罩边缘认真扶正。它不变大、不严肃，也不对用户进行第二次说教。

边界：认真模式必须由用户预先开启；不锁键盘鼠标，不改变显示器亮度，不模拟系统锁屏。紧急跳过始终位于最上层，位置固定，支持 Esc，不要求确认。

Reduce Motion：遮罩、角色和中央卡片原位交叉淡化。Reduce Transparency：改用不透明系统背景色，不依赖模糊保证可读性。

## 10. 正式角色需要持续满足的约束

已确认的正式角色在后续动作与实现中必须继续满足：

- 在 72–96 pt 尺寸下仍能一眼认出的轮廓。
- 能拿、拉、折、推 UI 零件的清楚肢体结构。
- 不靠复杂五官也能表达“收到、等待、让路、完成”。
- 正面、侧面和背面都可读，因为护眼动作需要角色主动背对用户。
- 可以携带一个很小的推车/夹子/印章系统，但不能变成道具堆砌。
- 静止时有性格，动作停止后不显得像普通几何占位符。

上述约束已经纳入静态设定和完整 MVP 动作关键帧；新增动作不得为了单个笑点破坏这些锚点。

## 11. MVP 动效优先级

第一批只制作八个最能建立特色的微互动：

1. 开始按钮收拢为进度核心。
2. 稍后折成预约票并夹回菜单栏。
3. 跳过盖章并归档。
4. 无回应折成待处理书签。
5. 第二次从书签展开新文案。
6. 站立逐步拆框并收成小推车。
7. 护眼把提醒卷成窗帘。
8. 小动作把提醒封成隐私信封。

完成反馈复用各陪伴隐喻的收尾动作，不另建一套大型庆祝系统。

## 12. 原生实现边界

提醒窗口保持固定尺寸，内部划分三层：

```text
GuideControlLayer       倒计时、进度、提前结束；始终可操作
CharacterLayer          角色与道具；不接收点击，从辅助功能树隐藏
DecorativeChromeLayer   可拆边框与纸片；不接收点击，从辅助功能树隐藏
```

- 点击后先写入 `snoozed`、`skipped`、`guided` 或 `ended_early`，再播放视觉反馈。
- 角色只接收语义提示：`acknowledgeSnooze`、`acknowledgeSkip`、`exitNoResponse`、`accompany(phase)` 和 `complete`。
- 20/30/60 秒都只在固定里程碑触发单次动作，不能每秒重新驱动角色动画。
- 动画被取消、资源缺失或开启 Reduce Motion 时，状态与业务计时完全不变。
- VoiceOver 不逐秒播报，只播报开始、必要的临近结束提示和完成。
- 多屏时只有一个可操作、可朗读的控制层；其他遮罩全部是装饰层。
