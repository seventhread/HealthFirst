# HealthFirst 隐私说明

最后更新：2026-08-28

HealthFirst 的当前版本以“本地优先”为原则设计。

## 当前版本处理的数据

- 提醒类型、间隔、持续时间、工作时段、文案风格与辅助功能偏好保存在本机的 macOS `UserDefaults` 中。
- 下一次提醒时间、间隔剩余时间、活动中的开始/稍后/再次/引导状态、待处理队列、全局暂停时间和暂停起点也会以带版本的数据保存在本机，用于应用重启后的可靠恢复。已被用户禁用的提醒类型、过期内容和其他不再有效的状态会在恢复时过滤或清理；引导倒计时不会把应用关闭的时间算作完成时间。
- 如果用户开启“登录后自动启动”，应用会请求 macOS `SMAppService` 在系统登录项中登记或取消登记 HealthFirst；该状态由 macOS 管理。
- 应用会接收 macOS 提供的睡眠、唤醒、锁屏和解锁等生命周期事件，用于重新安排本地提醒。
- 悬停、动画帧和短暂完成气泡等纯视觉状态只在应用进程内存在，不会在重启后恢复。

## 当前版本不会做的事情

- 不要求创建账号或登录。
- 不包含广告、分析 SDK 或遥测。
- 不上传设置、提醒记录或设备信息。
- 不访问通讯录、日历、照片、摄像头、麦克风或精确位置。
- 不在线生成提醒文字，也不从服务器下载角色或文案。

项目当前没有自有后端服务。由于源代码可以公开审查，任何未来加入的联网、分析或数据同步能力都应先更新本文件，并在产品中清楚告知用户。

## 删除本地数据

删除应用本身不会自动清除 macOS 保存的偏好。卸载前应先在 HealthFirst 设置中关闭“登录后自动启动”；如果系统仍显示该项目，可在“系统设置 > 通用 > 登录项”中关闭或移除。退出 HealthFirst 后，可以在“终端”中运行：

```bash
defaults delete app.healthfirst.macos
```

如果系统提示该偏好域不存在，说明没有可删除的数据。

## 问题与变更

隐私问题可以通过本仓库的 GitHub Issues 提出，但请勿在公开 Issue 中附上个人信息。安全漏洞请按照 [SECURITY.md](SECURITY.md) 中的方式报告。
